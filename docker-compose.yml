version: '3.1'

services:
  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: web
      MYSQL_USER: userdb
      MYSQL_PASSWORD: rahasia
    volumes:
      - ./db:/var/lib/mysql
